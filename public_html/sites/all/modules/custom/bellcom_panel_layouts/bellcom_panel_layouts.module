<?php

/**
 * Implements hook_ctools_plugin_directory()
 */
function bellcom_panel_layouts_ctools_plugin_directory($owner, $plugin_type) {
  if ($owner == 'panels' && $plugin_type == 'layouts') {
    return $plugin_type;
  }
}
